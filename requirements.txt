# Aula 05 - Orquestração com Airflow
# Estas deps são as ADICIONAIS instaladas dentro do container Airflow
# via _PIP_ADDITIONAL_REQUIREMENTS no docker-compose.yaml.
# O Airflow em si é instalado pela imagem oficial Apache Airflow 2.8.0.

scikit-learn==1.3.0
numpy==1.24.3
joblib==1.3.2
pendulum==2.1.2
